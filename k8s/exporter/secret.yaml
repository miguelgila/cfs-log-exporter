---
# API key shared between exporter and receiver.
# Replace the value with a strong random secret (base64-encoded).
#
#   echo -n "my-secret-key" | base64
#
apiVersion: v1
kind: Secret
metadata:
  name: cfs-log-exporter
  namespace: services
type: Opaque
data:
  api-key: Y2hhbmdlbWU=  # "changeme" — replace this!
